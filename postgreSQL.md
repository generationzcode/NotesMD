> Never finishing this because I got good at it without writing too much
- built on a client server model
- The server is where the datafiles are created and managed and stored
- The client uses software or directly makes connection to the database and makes requests and such
- [[creating a database]]
- [[SQL]]
- 